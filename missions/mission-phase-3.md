# Mission: Just Keep Identity (jki) - Phase 3 (Automation & Agency)

## 1. Project Context
JKI is a high-speed MFA vault in Rust. It is now in a stable **Standalone Mode**.
- **Data**: Split into `vault.metadata.json` (plaintext) and `vault.secrets.bin.age` (bulk encrypted via `age`).
- **Auth**: Hardened `acquire_master_key` supports 0600 files, env vars, and interactive toggle-indicator (`*->x`) input.
- **Paths**: `JKI_HOME` aware, absolute path resolution in `paths.rs`.
- **Quality**: >80% coverage across the workspace. Logic is decoupled into `handle_*` and `integrate_accounts` / `generate_otp` functions.

## 2. Objectives for this Session
### Objective A: Git Automation (`jkim sync`) [DONE]
- Implement `jkim sync` to perform an atomic backup flow: `git add` -> `git commit` (with auto-msg) -> `git pull --rebase` -> `git push`.
- Ensure it respects the `.gitignore` and `.gitattributes` generated during `jkim init`.

### Objective B: Agent Foundation (`jki-agent`) [DONE]
- Start the implementation of `crates/jki-agent`.
- Define the IPC protocol (likely Bincode or JSON over Unix Domain Sockets/Named Pipes).
- Implement the "Ping" and "GetOTP" command placeholders.

### Objective C: Metadata Editor (`jkim edit`) [DONE]
- Implement `jkim edit` following `crontab -e` pattern:
    - Create a secure temporary file (`0600`, `.tmp.json`).
    - Launch `$EDITOR`.
    - Validate JSON schema before overwriting the original vault metadata.

### Objective D: Master Key Management (`jkim master-key`) [DONE]
- Implement `set`, `remove`, and `change` commands.
- Support atomic re-encryption of the vault during `change`.
- Support manual input of old key if `master.key` file is missing.

## 3. Reference Files
- **Requirements**: `docs/jki-prd-spec.md` (V21).
- **Core Logic**: `crates/jki-core/src/lib.rs`.
- **CLI Logic**: `crates/jkim/src/main.rs`.
- **Status Report**: `missions/mission-unit-test-report.md`.

## 4. Engineering Standards
- Maintain high test coverage for new functions.
- Keep the "Pro" interactive feel (use `stderr` for messages, `stdout` for data).
- Ensure cross-platform compatibility (Unix/Windows).

---
*Generated by Gemini-CLI for Phase 3 handover.*
