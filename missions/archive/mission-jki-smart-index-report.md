# Report: jki 智慧序號解析與 Double Dash 保護 (Smart Index & Double Dash)

## 1. 任務執行摘要
根據 `docs/jki-cli-spec.md` 附錄 C 的規範，成功重構了 `jki` 的參數解析邏輯，實現了智慧序號提取、Double Dash (`--`) 保護以及優雅降級機制。

## 2. 實作變更
- **重構 `resolve_target` 邏輯 (`crates/jki/src/main.rs`)**:
    - 引入 `resolve_target` 輔助函數以提高可測試性。
    - **Double Dash 保護**: 當偵測到 `--` 時，強制將所有參數視為模糊搜尋模式。
    - **智慧序號提取**: 支援從參數列表中提取最後一個純數字作為候選序號，並優先嘗試套用於搜尋結果。
    - **衝突預檢與 Note 提示**: 在單一數字參數（如 `jki 14`）選中 Index 時，若該數字同時匹配其他帳號名稱（如 `FF14`），則在 stderr 輸出提示。
    - **優雅降級**: 若序號超出當前範圍，自動將該序號併入搜尋模式進行全文檢索。
- **單元測試**: 新增 4 個測試案例，覆蓋序號選取、Double Dash 保護、模式併入及降級邏輯。

## 3. 物理驗證結果
經由 `make release` 構建後執行手動驗證（Mock 環境下）：
1.  **`jki 1 -`**: 成功選中第 1 項，並正確輸出 Note 提示（因存在 Pattern 衝突）。
2.  **`jki -- 1`**: 成功執行模糊搜尋，選中名稱含有 "1" 的帳號而非第 1 項。
3.  **`jki 14 -`**: 當 14 超出範圍且存在帳號名稱含 "14" 時，成功降級為搜尋模式並選中該帳號。

## 4. 結論
本項任務已完成，`jki` 參數解析意圖識別已大幅優化，符合 CLI 高效操作之預期。

---
*Status: Completed by Gemini CLI Agent.*
